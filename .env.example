# Optional. RSS, discovery feeds, projects, bookmarks, and submissions work without keys.
SITE_URL=https://achievementwithai.com
# Add a real Buttondown publication API key in Vercel, then redeploy.
BUTTONDOWN_API_KEY=
NEWSLETTER_ENABLED=false
